name: Manutencao temporaria do artigo

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  aviso:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fluxo temporariamente suspenso durante a revisao editorial."
